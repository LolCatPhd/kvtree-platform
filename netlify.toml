[build]
  command = "npm run build"
  publish = ".next"
  base = "client"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"
  NEXT_PRIVATE_TURBOPACK = "0"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# No manual redirects here — let the plugin handle routing
